# ── Azure AD / SharePoint ──────────────────────────────────────────────────
msal>=1.28.0
requests>=2.31.0

# ── Configuration ──────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.7.0
pydantic-settings>=2.4.0

# ── Text extraction ─────────────────────────────────────────────────────────
langchain-community>=0.3.0
pypdf>=4.0.0          # PDF via PyPDFLoader
python-docx>=1.1.0    # .docx / .doc
openpyxl>=3.1.2       # .xlsx
python-pptx>=0.6.23   # .pptx

# ── Text splitting ─────────────────────────────────────────────────────────
langchain>=0.3.0
langchain-text-splitters>=0.3.0

# ── Embeddings ─────────────────────────────────────────────────────────────
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0
einops>=0.7.0
# CPU-only torch is sufficient for embedding generation in a batch job
torch>=2.0.0

# ── PostgreSQL + pgvector ──────────────────────────────────────────────────
psycopg2-binary>=2.9.9
pgvector>=0.3.2
numpy>=1.26.0
